<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Team RV" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : ""%>">
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= analytics_init if Rails.env.production? %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="<%= root_url %>">Team RV</a>
          <a class="pull-right" href="tel:+12294355852" style="float: left; padding: 10px 20px 10px;">(229) 435-5852</a>
          <div class="nav-collapse">
            <ul class="nav pull-right">
              <li class="<%= nav_li_class root_url %>"><%= link_to "Home", root_url  %></li>
              <li class="<%= nav_li_class rvs_path %>"><%= link_to "RVs", rvs_path  %></li>
              <li class="<%= nav_li_class parts_path %>"><%= link_to "Parts", parts_path  %></li>
              <li class="<%= nav_li_class services_path %>"><%= link_to "Services", services_path  %></li>
              <li class="<%= nav_li_class contact_url %>"><%= link_to "Contact", contact_path  %></li>
              <% if logged_in? %>
                <li><%= link_to "Logout", logout_path, method: "delete" %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span12">
              <%= bootstrap_flash %>
              <%= yield %>
            </div>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <hr>
        <p class="pull-right">
          <a href="#">Back to top</a>
        </p>
        <p>&copy; Team RV 2014</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "http://maps.googleapis.com/maps/api/js?sensor=false" %>
  </body>
</html>
